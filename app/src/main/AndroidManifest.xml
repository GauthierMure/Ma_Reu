<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.maru">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_logomar_u"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logomar_u"
        android:supportsRtl="true"
        android:theme="@style/Theme.Maréu"
        tools:targetApi="31">
        <activity
            android:name=".View.Activity.MeetingInfoActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activity.AddMeetingActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan"/>
        <activity
            android:name=".View.Activity.HomePageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>